/**
 * Placeholder pour le mode operateur live (Phase 6).
 * Interface simplifiee avec gros boutons tactiles et raccourcis clavier.
 */
export function OperatorPanel() {
  return (
    <div className="flex items-center justify-center w-screen h-screen bg-gray-950 text-gray-500 text-lg">
      Mode operateur (Phase 6)
    </div>
  );
}
